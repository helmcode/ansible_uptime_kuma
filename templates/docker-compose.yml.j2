version: "3.9"

services:
  uptime-kuma:
    container_name: uptime-kuma
    build: .
    volumes:
      - uptime-kuma:/app/data
    ports:
      - "3001:3001"
    networks:
      - net

networks:
  net:
